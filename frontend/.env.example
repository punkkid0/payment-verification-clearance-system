# Leave unset for local dev (defaults to http://localhost:5000/api).
# For a hosted frontend talking to a separate API:
# REACT_APP_API_URL=https://your-api-host/api
#
# For a single-service deploy (Express serves the React build):
# REACT_APP_API_URL=/api
REACT_APP_API_URL=
